import Color from "./Color";

export type Options = {
    height: number,
    width: number,
    inset: number,
    rounded: number,
    borderColor: Color,
    borderThickness: number,
    strokeColor: Color,
    strokeThickness: number
};

export default Options;
